{
  "name": "mediakit",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "description": "A backend utility package for optimising, processing, saving and serving both videos and images.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch 'src/**' --ext 'ts,json' --exec 'tsc'",
    "test": "jest --coverage --passWithNoTests --runInBand",
    "lint": "eslint src --fix"
  },
  "files": [
    "dist/**",
    "src/**"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "jest": "^26.6.3",
    "tsconfig": "*",
    "nodemon": "^2.0.16",
    "typescript": "^4.6.4"
  },
  "jest": {
    "preset": "scripts/jest/node"
  },
  "author": "",
  "license": "ISC"
}
